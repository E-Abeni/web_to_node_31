services:
  web:
    build: .
    ports:
      - "17004:17004"
    volumes:
      # Mounts only the main.py file from your host to the container
      - ./main.py:/app/main.py
    command: uvicorn main:app --host 0.0.0.0 --port 17004 --reload
